function minMax(boardField, player) {
  //looping trough gamefield and getting all position with empty spaces
  emptySpaces = boardField.filter((data) => data.values == ""); //looping trough gamefield and getting all position with empty spaces
}
